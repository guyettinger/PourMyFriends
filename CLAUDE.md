# Claude Code Context

This file is **not** the canonical source of project rules.
All authoritative instructions live in `AGENTS.md` at the repository root.

@AGENTS.md

## Claude-specific notes

- When in doubt, prefer yarn scripts from `package.json`.
- Use the MCP tools configured for this repo as described below.